{
  "version": 2,
  "framework": null,
  "installCommand": "pip install -r requirements.txt --break-system-packages",
  "buildCommand": "python manage.py migrate --noinput && python criar_usuarios_teste.py && python manage.py collectstatic --noinput",
  "outputDirectory": ".vercel/output/static",
  "env": {
    "DJANGO_SETTINGS_MODULE": "config.settings",
    "PYTHON_VERSION": "3.12"
  },
  "routes": [
    {
      "src": "/static/(.*)",
      "headers": { "cache-control": "s-maxage=31536000,immutable" },
      "continue": true
    },
    {
      "src": "/(.*)",
      "dest": "config/wsgi.py"
    }
  ]
}
